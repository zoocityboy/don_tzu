---
phase: 01-polish-ship
plan: '01'
type: execute
wave: '1'
depends_on: []
files_modified:
  - lib/features/manuscript/domain/repositories/manuscript_repository.dart
  - lib/features/manuscript/data/repositories/manuscript_repository_impl.dart
  - lib/features/manuscript/data/datasources/manuscript_local_datasource.dart
autonomous: true
requirements:
  - LANG-01
must_haves:
  truths:
    - Repository getManuscriptPages accepts language parameter in cs-CZ format
    - Language parameter returns proper translations based on locale
    - Unsupported locales fall back to language code only (cs-CZ → cs)
  artifacts:
    - path: lib/features/manuscript/domain/repositories/manuscript_repository.dart
      provides: Repository interface with language parameter
      method: getManuscriptPages(String language)
    - path: lib/features/manuscript/data/repositories/manuscript_repository_impl.dart
      provides: Repository implementation with language handling
    - path: lib/features/manuscript/data/datasources/manuscript_local_datasource.dart
      provides: Data source with language-aware lookup
  key_links:
    - from: getManuscriptPages(String language)
      to: manuscript_localizations.dart
      via: Locale parsing cs-CZ → cs
---

<objective>
Add language parameter support to manuscript repository for getting localized quotes. The repository should accept language in format cs-CZ (locale format with region code), parse it to language code, and return proper translations.
</objective>

<read_first>
- lib/features/manuscript/domain/repositories/manuscript_repository.dart
- lib/features/manuscript/data/repositories/manuscript_repository_impl.dart
- lib/features/manuscript/data/datasources/manuscript_local_datasource.dart
- lib/features/manuscript/l10n/generated/manuscript_localizations.dart
</read_first>

<tasks>

<task type="auto">
  <name>Add language parameter to repository interface</name>
  <files>lib/features/manuscript/domain/repositories/manuscript_repository.dart</files>
  <action>
Update getManuscriptPages method to accept language parameter (String locale, e.g., "cs-CZ"). Parse the locale to extract language code (cs-CZ → cs) and use that for localization lookup.

Method signature:
```dart
Future<List<ManuscriptPage>> getManuscriptPages(String language);
```

Parse locale format: If locale contains '-' (e.g., "cs-CZ"), extract language code before the dash. Fall back to full locale if no dash found.
  </action>
  <verify>
flutter analyze lib/features/manuscript/domain/repositories/manuscript_repository.dart
  </verify>
  <done>
getManuscriptPages accepts String language parameter and parses cs-CZ format
  </done>
</task>

<task type="auto">
  <name>Update repository implementation with language handling</name>
  <files>lib/features/manuscript/data/repositories/manuscript_repository_impl.dart</files>
  <action>
Update getManuscriptPages to accept language parameter and use it for localization:

1. Parse the language parameter: if contains '-', take first part (cs-CZ → cs)
2. Create Locale object from parsed code
3. Use ManuscriptLocalizations lookup with the Locale
4. Pass to datasource

```dart
@override
Future<List<ManuscriptPage>> getManuscriptPages(String language) async {
  // Parse locale: cs-CZ → cs
  final localeCode = language.contains('-') 
    ? language.split('-').first 
    : language;
  final locale = Locale(localeCode);
  final l10n = lookupManuscriptLocalizations(locale);
  // ... rest of implementation
}
```
  </action>
  <verify>
flutter analyze lib/features/manuscript/data/repositories/manuscript_repository_impl.dart
  </verify>
  <done>
Repository implementation handles language parameter and returns localized content
  </done>
</task>

<task type="auto">
  <name>Update datasource with language parameter</name>
  <files>lib/features/manuscript/data/datasources/manuscript_local_datasource.dart</files>
  <action>
Update the datasource to accept language string and pass it through to the localization system. The datasource receives the already-parsed Locale from the repository and uses it for lookup.

Update getManuscriptPages signature:
```dart
Future<List<ManuscriptPageModel>> getManuscriptPages(Locale locale);
```

And update the implementation to use the locale for loading correct translations.
  </action>
  <verify>
flutter analyze lib/features/manuscript/data/datasources/manuscript_local_datasource.dart
  </verify>
  <done>
Datasource properly receives and uses Locale for translations
  </done>
</task>

</tasks>

<verification>
Run flutter analyze to verify no errors.
Test with different locale formats: cs-CZ, de-DE, en-US.
</verification>

<success_criteria>
- getManuscriptPages accepts String language parameter
- Language format cs-CZ is parsed to cs
- Returns proper translations for the locale
- flutter analyze passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-polish-ship/01-01-SUMMARY.md`
</output>